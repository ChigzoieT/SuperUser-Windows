cd /c/Users/algorithm/Videos/superuser

cmake -G "MinGW Makefiles"

mingw32-make
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

#include "retrievedata.h"
#include <iostream>

void SomeFunction() {
    // The default server is "localhost" as defined in the CPP file.
    std::wstring userkey = L"exampleKey";
    
    PhpResponse resp;
    if (RetrievePhpResponse(userkey, resp)) {
        std::cout << "Text: " << resp.text << "\n";
        std::cout << "File: " << resp.file << "\n";
    } else {
        std::cout << "Failed to retrieve or parse PHP response.\n";
    }
}


